// // three types of variable in javascript
// 1.var // var is old variable 
// 2.let but let is new upadate variable both are same work
// 3.const const means we not change the value of run time and compile time

var a=10;
let b=20;
const c;//declare the variable 
c=30; //assign the variable
// c=90;// we not change the value of const 
document.write("I am const variable "+c);

// some rules of make variable 
// 1.we not use the keyword of js for declared the name of variable 
// we not give this name of variable as 1var